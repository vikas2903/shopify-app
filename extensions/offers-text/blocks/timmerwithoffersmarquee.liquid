<style>
  .announcement-bar {
    display: flex;
    align-items: center;
    background: {{ block.settings.background_color }};
    padding: 6px 15px;
    width: 100%;
    border-radius: 4px;
    box-sizing: border-box;
    overflow: hidden;
    gap: 20px;
  }

  .scrolling-text {
    width: calc(80% - 40px);
    overflow: hidden;
    white-space: nowrap;
    position: relative;
    font-size: 16px;
    font-weight: bold;
    color: {{ block.settings.text_color }};
    letter-spacing: 1px;
  }

  .scrolling-text-inner {
    display: inline-block;
    animation: scroll-left 15s linear infinite;
  }

  @keyframes scroll-left {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .right-section {
    width: 20%;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 16px;
    min-width: 220px;
  }

  .timer {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 4px;
    font-family: 'Segoe UI', Arial, sans-serif;
  }

  .timer-value {
    font-size: 20px;
    font-weight: bold;
    color: {{ block.settings.text_color }};
    letter-spacing: 2px;
  }

  .timer-labels {
    display: flex;
    flex-direction: row;
    gap: 12px;
    font-size: 11px;
    color: {{ block.settings.text_color }};
    margin-top: -3px;
    justify-content: space-between;
  }

  .cta-btn {
    background: {{ block.settings.button_background }};
    color: {{ block.settings.button_text_color }};
    font-weight: bold;
    border: none;
    border-radius: 4px;
    padding: 10px 14px;
    font-size: 14px;
    cursor: pointer;
    transition: background 0.2s;
    white-space: nowrap;
    text-decoration: none;
  }

  .cta-btn:hover {
    opacity: 0.9;
  }

  @media(max-width: 1200px){
    .right-section{ width: 40%; }
    .scrolling-text { width: calc(60% - 20px); }
  }
  @media (max-width: 700px) {
    .announcement-bar {
      flex-direction: column;
      align-items: stretch;
      padding: 0;
      width: 100%;
      gap: 0px;
    }
    .scrolling-text {
      width: 100%;
      font-size: 14px;
      padding: 10px 0;
    }
    .right-section {
      width: 100%;
      justify-content: center;
      min-width: unset;
      padding: 3px 0;
      border-top: 2px solid #ffffff;
    }
    .cta-btn{ padding: 4px 10px; }
    .timer span { line-height: 1.5; }
    .right-section{ justify-content: center; }
  }

{% if block.settings.enable_timer == false and block.settings.enable_cta_button == false %}
  .right-section {
    display: none;
  }
  .scrolling-text{width:100%;}
{% endif %}


</style>

<div class="announcement-bar">
  {% if block.settings.enable_marquee %}
    <div class="scrolling-text">
      <span class="scrolling-text-inner" id="scrollingText">
        {{ block.settings.announcement_text_1 }} &nbsp; &nbsp;
        {{ block.settings.announcement_text_2 }} &nbsp; &nbsp;
        {{ block.settings.announcement_text_3 }} &nbsp; &nbsp;
        {{ block.settings.announcement_text_4 }} &nbsp; &nbsp;
        {{ block.settings.announcement_text_5 }} &nbsp; &nbsp;
      </span>
    </div>
  {% endif %}

  <div class="right-section">
    {% if block.settings.enable_timer %}
      <div>
        <div class="timer" id="timer">
          <span class="timer-value" id="hours">00</span>
          <span style="font-size: 20px; font-weight: 600; color: {{ block.settings.text_color }};">:</span>
          <span class="timer-value" id="minutes">00</span>
          <span style="font-size: 20px; font-weight: 600; color: {{ block.settings.text_color }};">:</span>
          <span class="timer-value" id="seconds">00</span>
        </div>
        <div class="timer-labels">
          <span>Hrs</span><span>Mins</span><span>Secs</span>
        </div>
      </div>
    {% endif %}

    {% if block.settings.enable_cta_button %}
      <a href="{{ block.settings.button_link }}" class="cta-btn">{{ block.settings.button_text }}</a>
    {% endif %}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Marquee duplication
    const scrollingText = document.getElementById('scrollingText');
    if (scrollingText) {
      const text = scrollingText.innerHTML;
      scrollingText.innerHTML = text + text;
    }

    // 24-hour countdown reset daily
    function pad(n) { return n.toString().padStart(2, '0'); }

    function updateTimer() {
      const now = new Date();
      const endOfDay = new Date();
      endOfDay.setHours(23, 59, 59, 999); // End of current day
      let distance = endOfDay - now;

      const hours = Math.floor((distance / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((distance / (1000 * 60)) % 60);
      const seconds = Math.floor((distance / 1000) % 60);

      const h = document.getElementById('hours');
      const m = document.getElementById('minutes');
      const s = document.getElementById('seconds');

      if (h && m && s) {
        h.textContent = pad(hours);
        m.textContent = pad(minutes);
        s.textContent = pad(seconds);
      }
    }

    updateTimer();
    setInterval(updateTimer, 1000);
  });
</script>

{% schema %}
{
  "name": "Smart Announcement Bar",
  "target": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable_marquee",
      "label": "Show Marquee",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_timer",
      "label": "Show 24-Hour Timer",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_cta_button",
      "label": "Show CTA Button",
      "default": true
    },
    {
      "type": "header",
      "content": "Announcement Text Settings"
    },
    {
      "type": "text",
      "id": "announcement_text_1",
      "label": "Announcement Text 1",
      "default": "🚚 Free shipping on orders over ₹999"
    },
    {
      "type": "text",
      "id": "announcement_text_2",
      "label": "Announcement Text 2",
      "default": "🎁 Flat 60% Off on Rakhi Sale"
    },
    {
      "type": "text",
      "id": "announcement_text_3",
      "label": "Announcement Text 3",
      "default": "💥 Limited Time Offers"
    },
    {
      "type": "text",
      "id": "announcement_text_4",
      "label": "Announcement Text 4",
      "default": "🔥 Shop Now Before It Ends"
    },
    {
      "type": "text",
      "id": "announcement_text_5",
      "label": "Announcement Text 5",
      "default": "🎉 New Collection Launched"
    },
    {
      "type": "header",
      "content": "Color Settings"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#fff6e5"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "CTA Button Settings"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link",
      "default": "/collections/all"
    },
    {
      "type": "color",
      "id": "button_background",
      "label": "Button Background Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    }
  ]
}
{% endschema %}
